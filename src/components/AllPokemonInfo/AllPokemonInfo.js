
const AllPokemonInfo = () => {
    // const {pokInfo} = props;

    return (
        <div>
            <div className="type-of-pokemon">{
        //    pokInfo.map((info)=>
        //    {<li>{info.type}</li>})
            }</div>
        </div>
    )
}

export default AllPokemonInfo;